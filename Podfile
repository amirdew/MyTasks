# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyTasks' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyTasks
  pod 'Hero', '~> 0.3'
  pod 'CoreStore', '~> 4.0'


  target 'MyTasksTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
